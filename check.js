const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.invitationToken.findFirst({ include: { user: true } }).then(console.log).catch(console.error).finally(() => prisma.$disconnect());
